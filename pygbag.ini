[DEPENDENCIES]
# pygbag bundles the whole folder containing main.py. Keep the web bundle to
# just main.py + src/chokepoint by dropping the dev/venv/cache/test trees.
# (build/.git/.mypy_cache are excluded by pygbag's built-in list already.)
ignoreDirs = ["/.venv", "/.pytest_cache", "/.ruff_cache", "/.claude", "/tests", "/build"]
ignoreFiles = []
